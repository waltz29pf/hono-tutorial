# This file is generated by `lefthook install`
# and should be committed to your project.
#
# If you need to extend this functionality, please use `lefthook.local.yml`.
#
# See https://github.com/evilmartians/lefthook#readme

pre-commit:
  commands:
    biome-check:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx @biomejs/biome check --write {staged_files}
    biome-format:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx @biomejs/biome format --write {staged_files}
